 #include "main.h"

/**
 * 7-print_last_digit - Entry point
 * @num: used for checking
 * Return: the value of last digit
 */
int print_last_digit(int num)
{
	int a;

	if (num < 0)
		n = -n;
	a = n % 10;
}
